#ifndef JS_TOKEN_TYPES_H
#define JS_TOKEN_TYPES_H
typedef enum JS_TOKEN_TYPE
{
  {{
    join(map(tokenTypes, (@
      {{ $0 }}

    @)), ",")
  }}
} JSTokenType;

static const char * JS_TOKEN_TYPE_STR[] = {
  {{
    join(map(tokenTypes, (@
      "{{ $0 }}"
    @)), ",")
  }}
};
#endif
